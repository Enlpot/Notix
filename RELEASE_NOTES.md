# Notix Release Notes

## 8.0 (2026-08-22)

自上一已发布版本 7.47 以来的变更汇总。

### 新功能

- 存储占用：设置 > 常规 新增「存储占用」入口与二级明细界面，按类展示通知历史 / 规则 / 其他文件占用大小，支持单项清除与一键清除全部（均带二次确认）
- 按应用清除历史：弹窗改为深色卡片样式，列表项增加应用图标、整行点击多选、搜索过滤后一键全选/取消全选
- 按时间段清除历史：弹窗改为深色卡片样式，支持开始/结束日期选择与 7 天 / 30 天 / 90 天 / 全部快捷选项
- 新建规则三栏化：规则界面分为「来源 / 条件 / 工作流」三栏，顶部可填规则名称（为空自动命名「未命名规则N」）；条件通过弹窗配置（关键字 / 手机状态 / 时间三页签）；动作卡片支持拖拽排序、点击配置、长按删除
- 底部三 tab（History / Rules / Settings）切换过渡动画：淡入淡出 + 轻微水平位移（约 250ms），各页状态可保留

### 改进

- 规则界面：移除悬浮新建按钮，改为「规则」主标题 + 「共 N 条规则」副标题 + 顶部横幅新建按钮；规则卡片支持长按删除确认，移除卡片顶部删除按钮，保留重新扫描与开关；空态保留标题与横幅按钮
- 通知详情弹窗增宽（两侧 padding 缩至 12dp），固定显示删除 / 打开 / 创建规则 / 还原 四个按钮
- 聚合变更窗口右上角文案「变更 n」改为「n条变更」
- 通知历史界面柱状图独立为圆角深灰卡片，并增加 ◀/▶ 滑动提示箭头（横屏左栏同步）
- 通知历史搜索框展开后自动弹出键盘，关闭时同步收起
- 底部 tab 选中态改为主题色圆角胶囊样式（约 52x32dp、圆角 16dp），图标切换对比色

### 修复

- 通知折叠卡展开/收起后滚动位置错误：收起折叠段后自动回到段头
- 同应用多折叠段 key 冲突导致列表抛 IllegalArgumentException 崩溃
- 按应用折叠段改为按通知发送时间判定顺序，避免切换 tab 卡顿
