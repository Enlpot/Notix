# Notix Release Notes

## 8.6 (2026-08-23)

自上一已发布版本 8.5 以来的变更汇总。

### 改进

- 设置页权限整合重构：把原先散落在主设置页的「通知监听 / 电池优化 / 后台保活」三块引导，拆为统一的「权限管理」二级界面。主设置页对应位置变为单一可点击入口行，带盾牌图标、状态徽标（绿「正常」 / 红「N 项异常」）与右箭头；点入二级页查看 4 项系统权限的实时状态、说明与修复入口

- 新增对「发送通知（POST_NOTIFICATIONS）」与「前台服务保活」的实时监控，与原先的「通知监听」和「电池优化」共同组成 4 项监控

### 修复

- 权限状态卡在首次进入值：原实现用一次性 `remember {}` 求值，从系统设置返回后状态从不刷新。改为 `refreshTick` + 进入前台 / 返回时 `DisposableEffect(Lifecycle.ON_RESUME)` 自动重新检查，主入口与二级页均同步刷新

### 清理

- 删除 6 条已无引用的 `settings_keepalive_*` 文案（之前随旧版内联权限引导一同废弃）

## 8.5 (2026-08-23)

自上一已发布版本 8.4 以来的变更汇总。

### 修复

- 通知无法实时进入通知历史：H3 引入的内存缓存为实例级字段，导致监听服务写入后只刷新自身缓存、UI 实例缓存停留在首次读取的旧值，广播刷新命中陈旧缓存使历史不再实时更新。将缓存提升为进程级（伴生对象）共享，任意一次写盘立即可见（重大回归修复）
- 崩溃日志弹窗「打开日志位置」按钮文字折行：Material3 按钮默认内边距过大，半宽按钮内 6 字标题挤不下。收紧内边距并强制单行
- 崩溃日志弹窗「打开日志位置」点击无反应：原逻辑用 FileProvider 以 `text/plain` 打开日志文件（而非目录），无可用查看器即报错。改为优先用系统文件管理器打开日志目录，失败回退打开文件，再失败才显示路径

## 8.4 (2026-08-23)

自上一已发布版本 8.3 以来的变更汇总。

### 改进

- 历史存储改为原子写（AtomicFile）+ 后台线程即时增量落盘 + 历史更新广播节流，消除大历史文件主线程解析卡顿 / ANR 风险
- 监听服务环境快照 10s 缓存，避免通知高频到达时重复注册电量广播与枚举蓝牙音频设备，减轻监听线程负担

### 修复

- 历史折叠段展开状态按段级标识隔离，修复同包名多个折叠段被联动一起展开的问题（#38）
- 折叠提示数量错误：隐藏条数正确减去始终显示的首条，例如 4 条折叠段显示「收起 3 条」（#39）
- 设置向导进行中因旋转 / 配置变更被强制退出：重建时优先尊重已保存的向导显示状态（M1）
- Action Flow 宿主（监听服务）销毁后挂起回调仍继续推进副作用：注入宿主存活检查，销毁后静默终止（M2）
- 规则导入提示不准确：新增「无新规则（全部已存在）」明确提示，避免误导为「导入 0 条」（L1）
- 并发读写丢数据：StatsStorage 计数自增、UnmonitoredAppsStorage 增删改为加锁的读改写，修复高频命中丢计数 / 并发增删互相覆盖丢应用（L2）

## 8.1 (2026-08-22)

自上一已发布版本 8.0 以来的变更汇总。

### 改进

- 历史页多级吸顶：底部 tab 固定吸顶，统计区与折叠分组头吸附在 tab 下方，滚动时不再顶掉标签栏
- 规则向导简化：移除底部删除键、动作箭头与添加条件按钮，条件区固定三行展示，新建规则更聚焦
- 通知详情弹窗加宽（两侧 padding 缩至 12dp），固定显示删除 / 打开 / 创建规则 / 还原 四个按钮
- 启动图标重制：替换为自适应矢量图标（三杠灰）并细化缩放表现

### 修复

- 通知历史列表布局顺序错误
- 折叠段展开/收起后滚动位置错误：收起折叠段后自动回到段头
- 同应用多折叠段折叠计数错误
- 通知详情弹窗按钮显示异常
## 8.3 (2026-08-23)

自上一已发布版本 8.2 以来的变更汇总。

### 改进

- 通知详情弹窗加宽至屏幕真实宽度的 90%（关闭平台默认窄窗口，此前被压缩至不足 80%）
- 弹窗交互修复：点击外部遮罩关闭、半透明遮罩恢复，弹窗内部点击不再误关
- 聚合窗口（变更详情）同步改为 90% 宽度、点遮罩关闭，与详情弹窗视觉一致
- 全站深色模式配色规范为 Material Design 3 标准深色：基面由纯黑改为 `#1B1B1F`，主色 `#92CCFF`，文本/分割线走 MD3 中性色体系，对比度达 WCAG AAA
- 严格保留应用分组卡片、规则分组卡片、规则卡片的现有按应用派生配色，不做任何颜色更改

### 修复

- 通知详情弹窗点击外部无法关闭的问题（源于关闭平台默认窗口后原生 scrim 失效，已手动补遮罩）
- 仓库缺失 `Shape.kt`（`NotixCorner` 圆角令牌）导致 release 构建失败的引用错误
- Gradle daemon JVM 在 Windows 上的 CompressedOops 地址冲突（提升堆上限并迁移基地址至 4GB 以上）

## 8.2 (2026-08-23)

自上一已发布版本 8.1 以来的变更汇总。

### 改进

- 按钮体系统一：通知详情弹窗四按钮改为 Material3 Button 体系（删除红底/打开主题色/还原灰底/创建规则描边）；规则页、向导页、编辑页按钮统一 14dp 圆角
- 危险操作与圆角档位收敛：红底按钮/红字 TextButton 两种危险形态全项目一致；圆角统一为大容器 16dp、卡片 12dp、小组件 8dp
- 标题层级与弹窗统一：主页面 headlineMedium+Bold（设置页补齐主标题），次级页 titleLarge+返回键；冗余弹窗组件合并
- 图标规范化：周视图 ◀/▶ 与残留 Unicode 装饰字符替换为矢量图标

### 修复

- 移除设置页日期快捷入口与列表项不一致的描边按钮形态
- 通知详情弹窗冗余组件合并，删除未使用的 NotificationDetailsDialog
