# Notix 待办清单

> 用法：多项改进/bug 先登记于此，用户逐个提出，后续按顺序实施。方案确认后开工。

## 已完成

- [ ] 「按应用清除历史」弹窗改造（按参考图）
  - 来源：2026-08-22 用户需求（附参考图）
  - 方案（已确认）：
    - 弹窗样式：AlertDialog 改 Dialog + 深色卡片，标题「选择应用」+ 提示「勾选要清除历史记录的应用（多选）」；底部红色实心大按钮「清除所选应用历史」（现有 confirmButton 改全宽红底），右上角关闭按钮
    - 列表项：左侧加应用图标（RealAppIcon），中间应用名，复选框移到右侧；整行可点击切换
    - 搜索框右侧新增「全选」文字按钮：点击选中当前列表（filteredApps，含搜索过滤结果）全部应用；全部已选时再次点击取消全选（切换式）
    - 新增字符串：全选、选择应用、勾选提示、清除所选应用历史，多语言同步
  - 状态：已完成（2026-08-22 v8.0）

- [ ] 规则界面改造：标题+横幅新建按钮+长按删除
  - 来源：2026-08-22 用户需求（附参考图）
  - 方案（已确认）：
    - 布局：移除 Scaffold FAB，改 Column：顶部主标题「规则」+ 副标题「共 N 条规则」（新增字符串，多语言同步）→ 顶部横幅主题色圆角按钮「+ 新建规则」（复用 add_new_rule 文案）→ 规则列表
    - 删除改造：RuleCard 改为 combinedClickable，长按弹删除确认 AlertDialog（复用现有 confirm_delete_rule 弹窗逻辑）；移除卡片顶部 Delete IconButton；保留重新扫描按钮与 Switch
    - 空态：顶部仍显示标题+横幅按钮，下方保留空态引导（EmptyState 图标+文案，不再显示新建动作按钮）
    - 规则数 N = rules.size
  - 状态：已完成（2026-08-22 v8.0）

- [ ] 通知详情弹窗增宽 + 固定四按钮（删除/打开/创建规则/还原）
  - 来源：2026-08-22 用户需求（附参考图二）
  - 方案（已确认）：
    - 弹窗宽度：NotificationDetailDialog 两侧 padding 由 24dp 缩至约 12dp，内容区显示更多内容
    - 四按钮固定显示：删除 / 打开 / 创建规则 / 还原，所有场景均显示（含聚合窗口 HistoryNotificationDetailsDialog 弹出的详情；还原对未过滤通知也可用）
    - HistoryNotificationDetailsDialog 增加 onRestore 参数并透传给内部 NotificationDetailDialog（当前调用点 showRestore=false 需改 true）
    - 按钮样式按参考图二：删除=error 红底、打开=主题色底、创建规则=主题色边框透明底、还原=灰底，圆角矩形，FlowRow 自动换行保留
    - 字符串复用现有 notification_delete/open/create_rule/restore，无需新增
  - 状态：已完成（2026-08-22 v8.0）

- [ ] 设置界面增加「存储占用」入口 + 二级明细界面（按参考图）
  - 来源：2026-08-22 用户需求（附参考图）
  - 方案（已确认）：
    - 一级入口：设置「常规」分区新增「存储占用」行，显示总占用（自动计算 filesDir 下各文件实际大小，格式化 B/KB/MB 一位小数），右侧 NavChevron，点击进入二级界面
    - 二级界面：顶部返回栏「< 存储占用」；三项列表，每项 = 名称 + 占用值 + 右侧红色「清除」按钮，三项下方各加说明文字解释内容：
      - 通知历史：notification_history.json 等文件，清除 = 清空全部通知历史（复用现有 onClearHistory）
      - 规则：rules.json 等文件，清除 = 清空所有规则
      - 其他：崩溃日志、临时/备份文件（.bak/.corrupt/.tmp 等），清除不动 SharedPreferences 设置与统计
    - 每项清除前弹二次确认框；底部全宽红色「清除全部」按钮（同样二次确认），依次清空三项
    - 字符串同步多语言（zh-CN/en/es/fr/ja/ko/pl/ru）
  - 状态：已完成（2026-08-22 v8.0）

- [ ] 底部 tab 选中态改为主题色圆角胶囊样式（按参考图）
  - 来源：2026-08-22 用户需求（附参考图）
  - 方案（已确认）：
    - 选中态：主题色圆角胶囊背景（约 52x32dp，圆角 16dp）+ 图标改 onPrimary 深色对比色
    - 移除顶部 3dp 指示条，不保留底部短指示器（用户选择：胶囊已足够醒目）
    - 未选中态保持灰色图标不变；56dp 高度、长按气泡、3 tab 图标均不变
  - 状态：已完成（2026-08-22 v7.43）

- [ ] 底部三 tab（History/Rules/Settings）切换过渡动画
  - 来源：2026-08-22 用户需求
  - 方案：MainActivity 竖屏/横屏两处 `when(currentTab)` 内容区用 `AnimatedContent` 包裹（淡入淡出 + 轻微水平位移，约 250ms）；各页 rememberSaveable 状态可保留
  - 状态：已完成（2026-08-22 v8.0）

- [ ] 「清除历史」按时间段界面样式改造（按参考图样式）
  - 来源：2026-08-22 用户需求（附参考图）
  - 方案（已确认）：
    - 弹窗：AlertDialog 改为 Dialog + 深色 Material 卡片（与 v7.24 清除模式弹窗风格一致）
    - 标题「选择时间段」；说明文案「选择开始和结束日期（含首尾日）」
    - 开始/结束两个可点击日期选择项（点击弹出 DatePicker）
    - 新增四个快捷选项：7天 / 30天 / 90天 / 全部，普通按钮风格；按下后自动更新日期范围，**不直接清除**，仍需点清除按钮；「全部」= 日期范围覆盖全部历史（清除效果等同清除全部历史）
    - 底部全宽红色大按钮「清除此时间段历史」（error 色）；「取消」保留
    - 清除逻辑签名不变（onClearHistoryByDate(startMs, endMs)）；字符串同步多语言（zh-CN/en/es/fr/ja/ko/pl/ru）
  - 状态：已完成（2026-08-22 v8.0）

- [ ] 新建规则界面三栏化改造（来源/条件/工作流）
  - 来源：2026-08-22 用户需求（附参考图）
  - 方案（已确认）：
    - 布局：顶部新增规则名称输入框（占位「规则名称，如：屏蔽营销短信」）；主体改为三栏分组：来源 / 条件 / 工作流
    - 名称：复用 BlockerRule.description 字段，非必填；为空保存时自动命名「未命名规则N」（N 递增且与现有规则不重复）
    - 右上角对号：增加底色；canSave=false 时灰色无底色 disabled；canSave=true 时主题色高亮可点击（主题色非橙色）
    - 来源栏：入口卡片显示「来源应用」+ 已选应用（> 入口），点开仍为多选 AppPickerPanel，逻辑不变
    - 条件栏：改为「摘要展示 + 添加条件按钮」，点击弹窗打开配置窗口；弹窗含条件类型切换：关键字 / 手机状态 / 时间（参考图 1/2/3），底部取消/确定；原匹配条件（MatchModePicker+关键字输入）与手机状态额外条件（ExtraConditionCard）逻辑收入弹窗
    - 工作流栏：动作卡片列表；卡片可点击打开配置动作；卡片右侧拖动示意按钮，按住可拖动改变动作顺序（替换现有上移/下移按钮）；长按卡片（排除拖动按钮区域）弹出删除确认
    - 「+ 添加动作」按钮位于动作卡片下方
    - 字符串同步多语言（zh-CN/en/es/fr/ja/ko/pl/ru）
  - 状态：已完成（2026-08-22 v8.0）

- [ ] 通知历史界面柱状图分离改造（按参考图）
  - 来源：2026-08-22 用户需求（附参考图）
  - 方案：
    - 视觉分离：「通知历史」标题行 + 总通知/今日统计独立成顶部区；柱状图独立为圆角深灰卡片
    - 滚动结构不变：柱状图仍作为 LazyColumn 头部随界面滑出；三个分组 tab 保持吸顶
    - 柱状图左右增加小箭头（◀/▶），不可点击仅作滑动提示，尺寸小
    - 横屏左栏 ChartPanel 同步套用改造
  - 状态：已完成（2026-08-22 v8.0）

- [ ] 通知历史搜索框展开后自动弹出键盘
  - 来源：2026-08-22 用户 bug 反馈
  - 方案：HistoryScreen 搜索展开（searchExpanded=true）时，为 BasicTextField 设置 FocusRequester 请求焦点，并用 LocalSoftwareKeyboardController 弹出键盘；关闭搜索时同步隐藏键盘。AnimatedContent 过渡期间注意焦点请求时机（过渡完成后请求）
  - 状态：已完成（2026-08-22 v8.0）

- [ ] 聚合变更窗口右上角文案「变更 n」改为「n条变更」
  - 来源：2026-08-22 用户需求（附截图）
  - 方案：修改 values/strings.xml 中 `change_count` 字符串（现为「变更 %s」）→ zh-rCN 为「%s条变更」；默认 values 为英文（如「%s changes」）；es/fr/ja/ko/pl/ru 同步翻译。引用点：HistoryNotificationDetailsDialog.kt:109（`stringResource(R.string.change_count, entry.displayCount)`）
  - 状态：已完成（2026-08-22 v8.0）
