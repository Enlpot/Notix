# Notix 版本历史

> 本文件为本地累积版本历史的中文版，按版本倒序排列，内容与 `VERSION_HISTORY.md`（英文版）同步维护。
> 每次发布时，该版本的说明仅以**单独**形式作为 GitHub Release 正文发布（见 `RELEASE_NOTES.md`）。
> 本文件仅保留完整历史，不参与发布流程。

---

## 8.11 (2026-08-24)

**改进**
- 动作流拖动排序现在具备完整的动画指示：被拖卡片随手势抬升并产生让位间隙，释放后 spring 弹回原位。实现采用社区主流 `sh.calvin.reorderable` 库。

**修复**
- 清理与已删除 `SILENT` 动作相关的死代码：`SyncActionRunner.silent`、`ActionFlowHost.repostSilent`、`NotificationBlockerService.repostSilent`。同步更新单元测试桩（`FakeSyncRunner` / `FakeHost`）与相关断言。

**说明**
- `STRONG_REMIND`（强提醒）与 `POSTPONE`（延迟）动作在 v8.11 仍为 UI 壳层，真正的执行层将在后续版本接入。

---

## 8.10 (2026-08-24)

相对上一已发布版本 8.9 的变更。

### 优化

- 规则向导：点击动作卡片不再向下内联展开配置面板，而是以与全 app 统一的 `NotixDialog` 风格（标题栏 + X 关闭、92% 屏宽、点击外部空白关闭）弹窗打开。
- "添加动作"选择器升级为 `NotixDialog`，与其它弹窗视觉一致；底部「取消」按钮移除，依靠标题 X、外部点击或系统返回手势关闭。
- 动作卡片的拖动指示按钮改为独立的 48×64 dp 触控列、28 dp 图标，与"点击打开弹窗"区域在视觉和命中上彻底分离——点拖手柄或长按拖手柄不再意外打开配置弹窗。
- 规则向导的来源应用选择器：搜索框右侧新增上/下箭头，可折叠/展开下方应用列表；展开/折叠状态在配置变更后保留。

### 变更

- 动作目录与 Notix v1 规划对齐：从 7 项动作重构为 8 项——移除、点击按钮、打开、复制内容、TTS 播报、强提醒、等待、延迟。Label 按用户约定改名（"消除通知"→"移除"、"打开通知"→"打开"、"播报"→"TTS 播报"），删除"静默重显"。
- 本轮新增强提醒/延迟两类动作，**UI 与参数面板完整、可保存**：可设置强提醒的响铃/震动开关、延迟的毫秒数。但真正的执行层（高优 heads-up + 响铃 + 震动、Handler.postDelayed 重发原通知）计划在 v8.11 接入 NotificationProcessor + 前台服务；当前规则触发时这两类动作仅记录跳过日志、动作链继续推进不崩。

### 修复

- 修复了动作卡片的拖手柄误触打开弹窗的可访问性问题——打开弹窗的 clickable 目标与拖动排序的 pointerInput 目标已完全分离。
- 修复了规则向导弹窗样式与全 app 不一致的问题——"添加动作"选择器与每条动作的配置面板均统一为 `NotixDialog`，与设置页、权限管理、存储占用等弹窗同款风格。

---

## 8.7 (2026-08-23)

相对上一已发布版本 8.6 的变更。

### 优化

- 设置页：权限管理与存储占用改为居中弹窗显示，而非全屏二级界面，主设置页保持在弹窗后方可见。权限管理入口显示实时状态徽标（绿色"正常" / 红色"N 项异常"）。
- 存储占用弹窗现以"共占用"汇总头部开头。

### 变更

- 统一弹窗关闭交互：点击遮罩或关闭（X）按钮关闭；点击弹窗内部不再关闭。

---

## 8.6 (2026-08-23)

相对上一已发布版本 8.5 的变更。

### 优化

- 设置页权限整合重构：原先散落在主设置页的"通知监听 / 电池优化 / 后台保活"引导合并为单一"权限管理"二级界面。主设置页对应区域变为一个可点击入口行，含盾牌图标、状态徽标（绿色"正常" / 红色"N 项异常"）和右侧箭头；点击进入二级界面，展示 4 项系统权限的实时状态、说明与修复入口。
- 新增对"发送通知（POST_NOTIFICATIONS）"与"前台服务保活"的实时监控，与既有的"通知监听""电池优化"共同构成 4 项监控。

### 修复

- 权限状态卡片一度停留在初始值：原实现采用一次性 `remember {}` 求值，从系统设置返回后状态不再刷新。改为 `refreshTick` + `DisposableEffect(Lifecycle.ON_RESUME)`，在回到前台 / 恢复时自动重新检查，且在主入口与二级界面之间同步。

### 清理

- 移除 6 条未使用的 `settings_keepalive_*` 字符串（原先随旧的内联权限引导一同废弃）。

---

## 8.5 (2026-08-23)

相对上一已发布版本 8.4 的变更。

### 修复

- 通知未实时进入历史：H3 引入的内存缓存是实例级字段，监听服务写入后仅自身缓存刷新，UI 实例缓存停留在首次读取的陈旧值；广播刷新命中陈旧缓存，历史停止更新。将缓存提升为进程级（companion object）共享，任意一次磁盘写入立即可见（重大回归修复）。
- 崩溃日志弹窗"打开日志位置"按钮文字折行：Material3 按钮默认 padding 过大，半宽按钮中 6 字标题放不下。收紧 padding 并强制单行。
- 崩溃日志弹窗"打开日志位置"点击无反应：原逻辑用 FileProvider 以 `text/plain` 打开日志文件（非目录），无可用查看器时报错。改为优先用系统文件管理器打开日志目录，回退为打开文件，最后全部失败才显示路径。

---

## 8.4 (2026-08-23)

相对上一已发布版本 8.3 的变更。

### 优化

- 历史存储切换为原子写入（AtomicFile）+ 后台线程即时增量写入 + 节流的历史更新广播，消除大历史文件下的主线程解析卡顿 / ANR 风险。
- 监听服务环境快照缓存 10s，避免高频通知到达时重复注册电池广播与枚举蓝牙音频设备，降低监听线程负载。

### 修复

- 历史折叠分段展开状态按分段级 id 隔离，修复同一包名多个折叠分段被一起展开/收起的问题（#38）。
- 折叠提示计数错误：隐藏计数现正确减去始终显示的首项，例如 4 条折叠显示"收起 3"（#39）。
- 设置向导因旋转 / 配置变更被强制中途退出：重建时优先尊重已保存的向导可见状态（M1）。
- 动作流宿主（监听服务）被销毁但挂起回调继续推送副作用：注入宿主存活检查，销毁后静默终止（M2）。
- 规则导入提示不准确：新增明确的"无新规则（均已存在）"提示，避免误导为"导入 0 条"（L1）。
- 并发读写数据丢失：StatsStorage 计数器自增与 UnmonitoredAppsStorage 增删改为加锁的读-改-写，修复高频命中 / 并发增删互相覆盖导致计数丢失（L2）。

---

## 8.3 (2026-08-23)

相对上一已发布版本 8.2 的变更。

### 优化

- 通知详情弹窗加宽至真实屏幕宽度的 90%（关闭平台默认窄窗，原先被压缩到 80% 以下）。
- 弹窗交互修复：点击遮罩关闭、恢复半透明遮罩、点击弹窗内部不再误关。
- 聚合窗口（变更详情）同步至 90% 宽、点击遮罩关闭，与详情弹窗视觉一致。
- 全应用深色模式颜色标准化为 Material Design 3 标准深色：基面由纯黑改为 `#1B1B1F`、主色 `#92CCFF`、文字/分隔线遵循 MD3 中性色板，对比度达 WCAG AAA。
- 严格保留应用分组卡片、规则分组卡片、规则卡片既有的应用派生颜色——未做任何颜色改动。

### 修复

- 通知详情弹窗无法通过点击外部关闭（关闭平台默认窗口后原生遮罩失效；已补手动遮罩）。
- 仓库缺失 `Shape.kt`（`NotixCorner` 圆角令牌）导致引用错误使发布构建失败。
- Windows 下 Gradle daemon JVM CompressedOops 地址冲突（提高堆上限并将基址迁移到 4GB 以上）。

---

## 8.2 (2026-08-23)

相对上一已发布版本 8.1 的变更。

### 优化

- 统一按钮体系：通知详情弹窗中四个按钮改用 Material3 Button 体系（删除红色填充 / 打开主色 / 恢复灰色填充 / 创建规则描边）；规则页、向导页、编辑页按钮统一为 14dp 圆角。
- 整合危险操作与圆角层级：两种危险变体（红色填充按钮 / 红字 TextButton）全项目一致；圆角统一为大容器 16dp、卡片 12dp、小组件 8dp。
- 统一标题层级与弹窗：主页面 headlineMedium+Bold（设置页新增主标题），子页面 titleLarge+返回；合并冗余弹窗组件。
- 图标规范化：周视图 ◀/▶ 与遗留 Unicode 装饰字符替换为矢量图标。

### 修复

- 移除设置页日期快捷入口与列表项之间不一致的描边按钮变体。
- 合并通知详情弹窗中的冗余组件，删除未使用的 NotificationDetailsDialog。

---

## 8.1 (2026-08-22)

相对上一已发布版本 8.0 的变更。

### 优化

- 历史页多级粘性头部：底部 Tab 固定粘性，统计区与折叠分组头部吸附在 Tab 下方，滚动时不再把标签栏顶出。
- 规则向导简化：移除底部删除键、动作箭头、添加条件按钮；条件区固定三行，新建规则更聚焦。
- 通知详情弹窗加宽（侧边 padding 减至 12dp），修复删除 / 打开 / 创建规则 / 恢复四个按钮的显示。
- 启动图标重做：替换为自适应矢量图标（三栏灰色），缩放更精细。

### 修复

- 通知历史列表布局顺序错误。
- 折叠分段展开/收起滚动位置错误：收起后自动回到分段头部。
- 同应用多个折叠分段折叠计数错误。

---

## 8.0 (2026-08-22)

相对上一已发布版本 7.47 的变更。

### 新功能

- 存储占用：设置 > 通用 新增"存储占用"入口与二级详情界面，按类别展示通知历史 / 规则 / 其他文件的占用大小，支持逐项清除与一键全部清除（均带二次确认）。
- 按应用清除历史：弹窗重制为深色卡片；列表项现显示应用图标，支持整行点击多选，搜索过滤后一键全选 / 清空选择。
- 按时间段清除历史：弹窗重制为深色卡片；支持起止日期选择及快捷预设（7 / 30 / 90 天 / 全部）。
- 规则创建拆分为三栏：规则界面分为"来源 / 条件 / 工作流"；规则名可在顶部填写（为空时自动命名为"未命名规则 N"）；条件经弹窗配置（关键词 / 通话状态 / 时间标签页）；动作卡片支持拖拽排序、点击配置、长按删除。
- 底部三 Tab（历史 / 规则 / 设置）切换动画：淡入淡出 + 轻微水平位移（约 250ms），各页面状态保留。

### 优化

- 规则界面：移除悬浮新建按钮；改为"规则"主标题 + "共 N 条规则"副标题 + 顶部横幅新建按钮。规则卡片支持长按删除确认；移除卡片顶部删除按钮，保留重新扫描与开关；空状态保留标题与横幅按钮。
- 通知详情弹窗加宽（侧边 padding 减至 12dp），修复删除 / 打开 / 创建规则 / 恢复四个按钮的显示。
- 聚合变更窗口：右上角标签由"变更 n"改为"n 处变更"。
- 通知历史柱状图抽取为独立圆角深灰卡片，新增 ◀/▶ 滑动提示箭头（横屏左栏同步）。
- 通知历史搜索框展开时自动弹出键盘，关闭时收起。
- 底部 Tab 选中态改为主题色圆角胶囊（约 52×32dp，16dp 圆角），图标切换为对比色。

### 修复

- 通知折叠卡片展开/收起后滚动位置错误：收起后自动回到分段头部。
- 同一应用多个折叠分段键冲突导致列表中 IllegalArgumentException 崩溃。
- 按应用折叠分段现按通知发送时间排序，避免切换 Tab 卡顿。

---

## 7.47 (2026-08-21)

### 修复

- 修复 LazyColumn 中因同一应用出现在多个不连续折叠分段、折叠切换键重复导致的崩溃（IllegalArgumentException）；折叠分段键现包含该分段最新条目 id。

### 优化

- 修复历史 Tab 切换卡顿：从"已过滤"Tab 切到"按时间 / 按应用"不再在显示目标列表前冻结约 1 秒。数据准备现与所选 Tab 解耦，三个 Tab 共享同一份缓存数据集，切换不触发重算。
- 按应用折叠现按发送时间分段：应用组内仅当条目在全局时间线上相邻时才折叠在一起。若同一应用的两条通知之间插入了其它应用的通知，则形成独立折叠分段，而非一个大折叠组。

---

## 7.46 (2026-08-21)

### 新功能

- 历史界面新增通知卡片折叠：当某应用有 4 条及以上连续聚合条目时，折叠为单张卡片并带"展开 N 条"切换；三个 Tab（按时间 / 按应用 / 已过滤）均生效。展开/收起切换粘附在最新条目下方，一键折叠；展开卡片轻微缩进以区别于普通卡片。

### 优化

- 为新增折叠相关字符串新增西班牙语、法语、日语、韩语、波兰语、俄语翻译。

---

## 7.45 (2026-08-20)

### 新功能

- 新增可选"提取 remote views 文本"开关（默认关）：开启后，对无可见文本的通知，提取其操作按钮标签与内容描述，用于规则匹配与历史记录。

### 修复

- 修复历史界面"已过滤"与"按应用"Tab 间滑动卡顿：分组/排序计算现缓存，不再每次重组都全量重算。
- 修复切换 Tab 或列表回收后应用图标延迟出现：新增进程内应用图标内存缓存，避免重复访问 PackageManager。

---

## 7.43 (2026-08-19)

### 移除

- 移除未完成的"社区分享"功能：删除 CommunityShare.kt 及其在全部 8 种语言中的 80 条相关字符串资源。

---

## 7.42 (2026-08-18)

### 新功能

- 遵循 Material Design 3 深色配色的纯黑（OLED 友好）深色主题。
- 底部导航栏压缩为 56dp、仅图标项；长按图标显示其标签。
- 三个 Tab 统一横屏布局：左侧固定图表面板、右侧页面内容，中间以竖向分隔线分隔。

### 优化

- 横屏模式下选中日期状态跨旋转保留（共享图表面板）。
- 相邻历史子 Tab 预组合，避免滑动时白屏闪烁。
- 底部 Tab 现仅支持点击切换，顶部子 Tab 仍保留滑动。

### 修复

- 修复横屏界面显示旧版灰色窗口背景而非纯黑的问题。

---

## 7.39 (2026-08-18)

### 新功能

- 历史 Tab 现支持三个子 Tab 间滑动导航（HorizontalPager）。
- "已过滤"Tab 按命中的规则对通知记录分组。

### 优化

- 历史列表在通知较少时仍可滚动（修复短内容时无法滚动的问题）。
- 双击底部导航"历史"Tab 快速回到本周（清除日期过滤）。

### 修复

- 修复规则向导在屏幕旋转时状态丢失的问题。

---

## 7.38 (2026-08-18)

- 初始发布说明条目。完整更新日志：https://github.com/Enlpot/Notix/commits/v7.38
- 通知详情弹窗按钮显示异常。
