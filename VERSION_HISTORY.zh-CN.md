# Notix 版本历史

> 本文件是本地的累积版本历史，按最新在前排序。
> 每次发版时，该版本的说明会作为 GitHub Release 正文**单独**发布（见 `RELEASE_NOTES.md`）。
> 本文件仅保留完整历史，不参与发布。

---

## 8.57.1（2026-09-11）

**修复**
- **增强搜索关闭后列表仍处于搜索态**：退出搜索时重置 `advancedMode` 并清空多字段条件，避免关闭后仍按增强结果过滤（条件空时整页空白）。

---

## 8.57.0（2026-09-03）

**移除**
- **删除通知热词词云整条链路**：统计页词云卡片、词频 Room 表（v6→v7 `DROP TABLE word_frequency`）、增量/重建逻辑、`WordCloud` 组件
- **删除 HanLP 分词插件体系**：`WordTokenizerManager`、插件模块 `:plugins:word-tokenizer`、设置页插件市场/镜像源/每日重建词频
- **移除 `INTERNET` 权限**：该权限仅为插件下载服务；应用恢复「核心完全离线」声明
- **存储占用去掉「插件」分类**；权限页去掉「网络访问」卡片

**说明**
- 历史通知、规则、统计（总量/趋势/健康度等）不受影响；已安装过插件的设备会留下 `filesDir/plugin_*` 残留目录，可用系统「清除应用存储」或后续版本清理

---

## 8.56.0（2026-09-02）

**新增**
- **存储占用新增「插件」分类**：分词插件（HanLP 词典与数据）占用单独展示，目录递归统计
- **权限管理补全「网络访问」**：INTERNET 权限（插件下载、镜像源测速、更新检查），普通权限安装即授予
- **恢复常驻通知改为列表弹窗**：显示被规则冻结的常驻通知（应用名），支持单独恢复与全部恢复

**修复**
- **设置页存储占用更新不及时**：进入设置页/返回前台/打开弹窗时自动刷新，不再显示旧值

---

## 8.55.3（2026-09-02）

**优化**
- **红心收藏状态判断更可靠**：优先读取标准 API（MediaMetadata USER_RATING + HeartRating，无歧义），读不到再退回 PlaybackState 自定义动作推断（覆盖 like/favorite/heart/thumbs_up 等常见命名）

---

## 8.55.2（2026-09-02）

**新增**
- **红心按钮显示收藏状态**：媒体通知详情中「喜欢」按钮——当前歌曲已标红心时填充红色，未标保持主题色；点击后立即切换并自动校准（依据 MediaSession 自定义动作推断，可能因音乐 app 而异）

---

## 8.55.1（2026-09-02）

**新增**
- **媒体通知「当前状态」定制"正在播放"**：音乐正在播放时显示「正在播放」（优先于取消原因），解决系统收纳媒体通知后误显示「应用取消」

**优化**
- **进度条改为细轨道 + 小圆点样式**：更紧凑（参考 YouTube Music 等主流播放器），上下高度明显减小，仍可点击/拖动快进

---

## 8.55.0（2026-09-02）

**新增**
- **媒体控制改为直接枚举 MediaSession（方案B）**：不再依赖通知 posted 缓存。部分系统（ColorOS 16 等）会把音乐通知收纳进控制中心媒体卡片、不在通知栏显示，导致媒体控制与「当前状态」失效。现在直接从系统活跃 MediaSession 源头取：播放/暂停/上一首/下一首/进度条、播放暂停图标实时切换均可用

**修复**
- **「当前状态」误判「应用取消」**：音乐正在播放但通知被系统收纳时，原逻辑误判为已取消。现对媒体通知改用 MediaSession 活跃度判断，正在播放即显示「正显示」

---

## 8.54.3（2026-09-02）

**修复**
- **媒体控制点击按钮时弹窗闪一下**：重新连接媒体会话时进度条被短暂置空导致弹窗闪烁。修复 DisposableEffect 清理逻辑，重连时不置空状态，进度条不再消失

---

## 8.54.2（2026-09-02）

**修复**
- **媒体控制图标不实时切换**：点击播放/暂停等按钮后图标不随播放状态变化（MediaController 回调在会话更新时失效）。改为每 1 秒主动读取播放状态 + 点击按钮后自动重新连接媒体会话，图标与进度实时同步

---

## 8.54.1（2026-09-02）

**新增**
- **媒体控制播放进度条**：通知详情弹窗的媒体控制新增可拖动进度条（显示 当前/总时长），拖动可跳转到指定播放位置

**修复**
- **播放/暂停图标不自动切换**：服务重启后媒体会话缓存被清空，详情弹窗拿不到播放状态。改为打开弹窗后每 1 秒轮询重试获取媒体会话，拿到后自动连接，播放/暂停图标随状态实时切换

---

## 8.54.0（2026-09-02）

**新增**
- **通知详情弹窗媒体控制**：对带操作按钮的通知（如音乐播放），在详情弹窗显示 喜欢/上一首/播放/下一首/歌词 等操作按钮，点击等价于点系统通知上的按钮；播放/暂停按钮按当前播放状态切换图标
- **播放/暂停动态图标**：通过 MediaSession 读取播放状态，播放中显示暂停图标，暂停时显示播放图标

**修复**
- **正在播放的通知显示「已取消」**：ColorOS 等系统会过滤前台服务通知的实时查询（getActiveNotifications），导致正在播放的音乐通知在详情被误判为「已取消」。改为 Service 自维护通知栏状态（posted/removed 回调维护 + 媒体会话与操作按钮缓存），不再依赖被系统过滤的实时查询

---

## 8.53.1（2026-09-02）

**修复**
- **无文字通知漏记录**：中国移动云盘等使用自定义 RemoteViews 的通知（无 `android.title`/`android.text`）此前被「无文字即忽略」逻辑直接丢弃，不进入通知历史。现改为总是尝试提取自定义视图/按钮文字（移除原「提取无文本通知文字」设置开关）；提取失败时用「应用名 + 渠道名」作标题兜底记录，保证这类通知不再漏记

---

## 8.53.0（2026-09-02）

**修复**
- **前台服务通知被误判为普通通知**：音乐播放（如网易云）、导航、录音等前台服务通知 flags 为 `NO_CLEAR|FOREGROUND_SERVICE`，未设置 `FLAG_ONGOING_EVENT`，原逻辑只认后者导致误判为普通通知，每次刷新都新增记录、堆积大量播放通知。现改为 `FLAG_ONGOING_EVENT` 或 `FLAG_FOREGROUND_SERVICE` 任一命中即判为常驻通知，播放期间所有刷新走生命周期合并（只更新一条，不新增计数），停止后再播放才算新记录

---

## 8.52.3（2026-09-02）

**优化**
- **「当前状态」文案调整**：用户滑除通知的状态由「已清除」改为「用户移除」，与其他状态语义更统一（如「用户点击」「应用取消」）

---

## 8.52.2（2026-09-02）

**修复**
- **通知详情「当前状态」显示错误**：同 sbnKey（如高频刷新 / 聚合折叠）存在多条记录时，取消原因只写入最新一条，其余记录为 null 导致详情误显示「已结束」。现改为按 sbnKey 全量更新取消原因（聚合/折叠内任意记录均正确显示，用户滑除显示「已清除」等）\n- **取消原因写库后未刷新界面**：取消原因更新后未广播通知 UI，打开详情读到旧快照误显示「已结束」。现写库后补发 HISTORY_UPDATED 广播，界面实时刷新

---

## 8.52.1（2026-09-02）

**优化**
- **规则卡片命中计数位置调整**：命中次数移至第二行（app 图标 + app 名称行）右侧，与重置按钮并排；无命中显示「无命中」，命中显示「命中N次」
- **重置按钮改纯图标**：规则卡片重置命中计数按钮由文字按钮改为纯图标按钮，始终显示
- **移除独立命中计数行**：删除规则卡片底部独立的命中次数行，布局更紧凑
- **规则条件区对齐**：关键字/状态/时间 条件多行展示时，左侧标签改为与内容第一行顶部对齐 (2026-09-02)

---

## 8.52.0（2026-09-02）

**优化**
- **通知卡片 App 图标融入标题行**：移除卡片左侧 28dp 大图标，改为在标题行 app 名称左侧展示 14dp 缩小版真实图标，卡片主体保持纯色背景，信息更紧凑 (2026-09-02)

---

## 8.51.1（2026-09-02）

**优化**
- **漏斗徽标样式统一**：卡片右下角「已过滤」漏斗徽标改为扁圆（10dp 圆角），与变更聚合窗口一致
- **漏斗徽标右对齐**：与卡片右上角变更计数徽章右边缘对齐，消除视觉错位 (2026-09-02)

**新增**
- **通知详情「当前状态」增强**：准确区分通知取消原因（用户清除、全部清除、应用取消、应用清空、超时消失、通知渠道关闭、应用被禁用、规则命中等 20+ 种），通过系统真实取消原因码获取，替代笼统的「已结束」

**优化**
- **规则命中状态**：任何规则命中（移除 / 复制 / TTS / 强提醒等）的通知详情统一显示「规则命中」，并优先于后续取消原因（被规则处理过 > 后来被清除）
- **「已过滤」Tab 更名「按规则」**：语义更准确，与「按时间」「按应用」并列
- **「已过滤」Tag 改纯图标**：卡片与详情中的「已过滤」文字标记改为过滤漏斗图标（红色语义保留），更紧凑直观 (2026-09-02)

**新增**
- **动作流长按替换**：长按动作卡片弹出「替换 / 删除」操作菜单，选择「替换」后可用其他动作类型替换当前动作（替换后按新类型生成默认参数并进入参数编辑），删除仍保留二次确认

**优化**
- **添加动作卡片置顶**：动作流「添加动作」改为卡片样式置顶展示，与其他按钮卡片（来源应用 / 配置条件）风格统一，并去掉重复的「+」号
- **移除空态提示**：动作流为空时不再显示「暂无动作，点击下方按钮添加」提示文案

---

## 8.49.1（2026-09-02）



## 8.49.0（2026-09-01）

**新增**
- **增强搜索**：通知历史搜索支持「简单 / 增强」两种模式切换（搜索框左侧漏斗按钮）。增强模式可对 App 名称、包名、标题、内容、通知渠道多字段进行精准组合查找（多字段同时填写 = AND 组合），并支持按时间范围（不限 / 今天 / 本周 / 本月）筛选；搜索覆盖全部历史数据，不受列表分页限制

---

## 8.48.4（2026-09-01）

**优化**
- **常驻通知跨生命周期内容去重**：常驻通知被移除后重新出现时，若标题、内容与最新记录完全一致（如系统悬浮窗提示等低频重复提醒），不再新增变更记录、不累加计数，仅将时间更新为最新；仅当内容发生变化时才记录为新变更，保留连接状态变化痕迹

---

## 8.48.3（2026-09-01）

**新增**
- **常驻通知生命周期合并**：同一常驻通知（ongoing）持续高频刷新（如流量、下载进度等每秒变化）时，合并为一条记录，不再累加变更计数；只有该通知被移除后重新出现（断开重连）才新增一条记录，保留连接状态变化痕迹
- **设置项**：通知栏新增「常驻通知合并刷新」全局开关（默认开启），以及「高频常驻应用」设置（可单独对某个应用设置是否合并，覆盖全局开关）

---

## 8.48.2（2026-09-01）

**修复**
- **词云热词重叠**：通知热词词云中多个词语堆叠在一起的问题，布局算法改为分层环形扫描，放不下的词直接跳过，不再堆叠在中心（保证零重叠）

---

## 8.48.1（2026-09-01）

**修复**
- **常驻通知卡片时间不刷新**：常驻通知（ongoing）标题、内容不变仅再次更新时，历史卡片显示时间现在会同步刷新到最近一次更新（不再停留在首次记录时间），位置排序与时间戳保持一致
- **首次启动同步现有通知的时间错误**：App 首次启动或监听重连时，将通知栏现有通知补录进历史的时间现在按通知实际投递时间记录（此前显示为启动/同步时刻，导致补录的卡片时间全部变成启动时间）

---

## 8.48.0（2026-09-01）

**新增**
- **统计页模块开关**：统计页右上角新增设置按钮，弹窗内可用 Switch 独立开关各统计模块（通知热词/通知健康度/通知趋势/24小时分布/App通知占比/规则效果排行/App通知排行），关闭后对应卡片不再显示
- **热词模块依赖分词插件**：未安装分词插件时热词模块自动隐藏，模块弹窗中该开关置灰并提示"需安装分词插件"

**优化**
- **HanLP 分词效果大幅增强**：为 HanLP 内置现代汉语常用词表（第2版 5.6 万词）+ 网络流行新词 + 通知高频词，共约 3.5 万词，替换原 1998 年小词典，现代词/网络词识别显著改善
- **移除内置简单分词器**：不再内置兜底分词器，未安装分词插件时热词统计静默跳过（无数据），避免劣质分词结果干扰词云

---

---

## 8.47.4（2026-09-01）

**新增**
- **下载速度动态兜底**：实际下载速度持续远低于测速值（低于测速 30%，持续 10 秒）时，自动终止当前下载并切换下一线路，避免被"测速快但实际限速"的镜像源拖时间；探测字节过少的源测速值不可靠，不严管（仅用 5KB/s 兜底）

**优化**
- 切换线路时状态明确提示：`XX 源失败（下载速度不达标（实际 X KB/s，测速 Y KB/s）），切换下一镜像源`

---

---

## 8.47.3（2026-09-01）

**新增**
- **智能线路探测**：安装插件时并行下载每个源的前 256KB 测速（合计至多 1.25MB，带宽竞争极小），3 秒内自动选出最快线路继续完整下载，其余源即时终止并清理临时文件
- **坏源提前识别**：返回假 zip（如代理返回 2KB HTML）的镜像源在测速阶段即被识别剔除，不再等到解压才发现

**优化**
- 下载状态文案更新：`正在检测最优线路…` → `已选定线路：XX` → `正在从 XX 源下载` → `下载中 进度/速度` → `下载成功` → `解压中` → `加载中` → `安装完成/失败原因`

---

---

## 8.47.2（2026-09-01）

**新增**
- **镜像源延迟并行测试**：5 个镜像源同时测延迟，总耗时≈最慢源（上限 5 秒），原来顺序测 5 源全超时需 25 秒，安装插件启动更快

**修复**
- **插件加载失败不再反复下载**：解压成功即 zip 有效，加载失败属于插件依赖/环境问题，直接报出失败原因，不再每个源重新下载 7MB
- **插件 dex 打包 kotlin-stdlib**：根治 release 版 app 上插件安装"解压成功但加载失败"（此前插件 dex 缺 Kotlin 标准库，release 版 R8 裁剪后运行时 ClassNotFoundException），新插件包已重建发布

---

---

## 8.47.1（2026-08-31）

**新增**
- **插件安装状态单行显示**：安装过程实时显示当前状态（尝试源→失败切换→下载中速度/进度→下载成功→解压中→加载中→安装完成/失败原因），单行覆盖
- **下载速度与进度显示**：下载中显示实时平均速度（如 1.2MB/s）与百分比，附进度条

**优化**
- **换源前断开旧连接**：切换镜像源前确保前一个下载连接已断开并支持取消，杜绝重复下载与连接泄漏；协程被取消时立即终止整个流程
- **安装按钮改加载圆圈**：点击安装后按钮变为加载动画，安装完成恢复

**修复**
- **调试日志打印完整 cause 链**：崩溃/异常日志最多输出 6 层 Caused by，插件加载失败（InvocationTargetException 等包装异常）可看到真正根因（如 OOM）

---

---

## 8.47.0（2026-08-31）

**新增**
- **调试日志系统**：新增调试日志（debug_log.txt），详细记录插件下载/解压/加载、通知接收/规则匹配/动作执行、应用启动等关键链路，带设备信息（品牌/型号/系统版本/内存），用于远程诊断
- **崩溃后自动开启调试日志**：检测到上次启动崩溃时，下次启动自动开启调试日志，便于复现问题过程
- **设置页调试日志入口**：支持开关、查看日志、打开存放位置、分享日志文件、清空（二次确认）

**优化**
- **插件加载失败自动切换镜像源**：加载失败记录具体原因，坏插件目录自动清理并自动切换下一镜像源重试，不再反复卡死
- **崩溃日志附设备信息**：崩溃记录携带设备品牌/型号/系统/版本/内存等关键信息
- **插件/通知/规则链路埋点**：镜像源、插件加载、通知接收、规则命中、动作流等关键路径均写入调试日志

**修复**
- **初始化顺序**：调试日志最先初始化，确保插件加载等启动期埋点可完整记录

---

---

## 8.46.1（2026-08-31）

**新增**
- **插件与主版本解耦**：插件改为独立 workflow 构建，上传到固定 release（plugin-hanlp），下载 URL 恒定；主版本发布不再捆绑插件构建
- **镜像源管理**：镜像源列表上限放宽到 10 个；内置 6 个实测可用的默认镜像源（ghfast.top / gh-proxy.com / gh.llkk.cc / gh.ddlc.top / ghproxy.cn / ghproxy.link），替换原不可用的默认源

**优化**
- **添加镜像源流程简化**：无需先手动"测连通性"，输入前缀直接添加，添加后自动测试连通性并更新列表
- **提示信息层级**：应用内提示改用系统 Toast 显示，弹窗打开时提示不再被弹窗阴影遮挡

---

---

## 8.46.0（2026-08-31）

**新增**
- **插件市场**：设置页插件栏新增「添加插件」入口（置顶），点击打开插件清单弹窗，支持搜索、查看插件介绍、一键安装；安装后插件以卡片形式出现在插件栏，右侧提供卸载按钮（二次确认）。插件清单采用可扩展数据模型，为后续更多插件做准备

---

---

## 8.45.0（2026-08-31）

**新增**
- **规则卡片支持折叠/展开**：头部区（规则名称行 + 来源 App 行）单击折叠/展开单条规则，折叠后仅显示标题与来源两行；内容区（条件/动作流/命中次数）单击进入规则编辑界面
- **双击底部「规则」tab 全量切换**：300ms 内双击规则 tab 将所有规则一次性折叠或展开（与通知历史 tab 双击行为一致）

**优化**
- **折叠状态保持**：折叠/展开状态在切换 tab、切换界面、退到后台后均保持，完全重启应用后恢复默认展开

---

---

## 8.44.6（2026-08-31）

**修复**
- **规则页"周7"显示错误**：时间条件摘要中"周日"误显示为"周7"（星期名称映射越界），已修复为正确的"周一~周日"
- **规则列表排序未生效**：规则按创建时间倒序排列（最新创建在最上方），此前排序逻辑写错位置未作用于列表
- **强提醒动作摘要不跟随配置**：强提醒摘要此前固定写死"响铃+震动"，现按用户配置的响铃/震动开关动态显示，"heads-up"术语改为中文"横幅"

**优化**
- **规则卡片动作流全部展示**：动作流不再截断省略（原只显示前 3 个 + "…"），全部动作用箭头连接展示并自动换行，去掉"共 N 个动作"冗余尾巴
- **动作文案优化**：复制动作统一"复制"前缀（"复制标题和正文"）；TTS 播报模板中的占位符转为可读词并用「」包裹（如"「应用」「标题」"）
- **规则卡片条件区排版**："关键字"标签不再换行（自动扩宽 + 单行限制）；关键字值字体与状态、时间统一
- **规则卡片结构**：动作流上方增加分割线，与条件区区分

---

---

## 8.44.5（2026-08-31）

**修复**
- **release 包动作参数丢失（严重）**：规则动作流的参数（点击按钮名 / 复制模式 / 延迟时长 / TTS 模板 / 强提醒设置 / 延迟重发时长）在正式发行版中全部丢失，debug 包正常。
  - 根因：release 开启 R8 混淆但 proguard 规则为空，参与 Gson 反射序列化的 data class 字段被混淆改名（如 `ClickButtonParams.buttonLabel` → `a`），序列化输出混淆后的 JSON key，读取端按原始字段名查不到 → 参数丢失
  - 修复：`proguard-rules.pro` 增加 keep 规则，保护规则/动作规格/条件/动作参数 data class 字段名 + 枚举常量名
  - 真机 release 复测：按钮名保存、回显正常

---

---

## 8.44.4（2026-08-31）

**修复**
- **胶囊开关关闭状态看不清**：开关关闭时的边框和滑块颜色加深（outlineVariant → outline），浅色/深色背景下关闭状态均清晰可见（设置页 / 规则卡片 / 规则向导等所有开关统一生效）。

---

---

## 8.44.3（2026-08-31）

**新增**
- **通知详情弹窗高度自适应**：内容少时弹窗自动收矮、内容多时顶到 0.6 屏高上限后内部滚动，不再固定撑满大窗口（单条通知详情 / 变更聚合弹窗一并生效）。

**修复**
- **通知详情 / 变更聚合弹窗闪退**：修复打开通知详情或变更计数弹窗即崩溃的问题。
  - 根因：弹窗内容区使用 weight(fill=false) 导致滚动组件收到无限高度约束（IllegalStateException）
  - 修复：内容区改用 heightIn(max) 有界约束，同时实现高度自适应
- **规则向导匹配模式 / 关键字输入弹窗闪退**：修复创建规则时打开"匹配模式"或"输入关键字"弹窗崩溃的问题（v8.43.1 引入的嵌套滚动，content 内滚动组件套外层滚动容器）

---

---

## 8.44.2（2026-08-31）

**修复**
- **设置页权限监控弹窗闪退**：修复真机上打开"权限管理"（权限监控）弹窗即崩溃的问题。
  - 根因：v8.43.1 为弹窗内容区新增垂直滚动后，权限监控等弹窗内的 LazyColumn 列表被嵌套进外层滚动容器，触发"无限高度约束"（IllegalStateException）崩溃
  - 修复：NotixDialog 新增 contentScrollable 参数，含 LazyColumn 的弹窗（权限监控 / 清除按应用 / 未监控应用）不再套外层滚动，根治嵌套滚动崩溃
  - 属通用修复，后续弹窗内容使用 LazyColumn 均不再受影响

---

---

## 8.44.1（2026-08-31）

**修复**
- **API 24~32 设备启动闪退**：修复 `LocalDate.ofInstant`（仅 Android 13+/API 33 及以上提供）在低版本设备上导致的 `NoSuchMethodError` 崩溃。
  - 启用 core library desugaring（desugar_jdk_libs 2.1.5），`java.time` 全系 API 在 minSdk 24 上完整可用
  - 修复统计页/通知历史页日期逻辑在 Android 12 及以下设备启动即崩溃的问题

---

---

## 8.44.0（2026-08-31）

**新增**
- **高级分词插件（HanLP）**：设置页新增"高级分词插件"入口，用户可自行安装 HanLP 高级分词引擎，安装后统计页通知热词词云的分词更精准（正确切分"会议室""快递""人工智能"等复合词）。
  - 插件采用单文件 zip 分发（dex + 词典），默认安装包不含插件，体积不变
  - 点击安装后全自动完成：下载 → 解压 → 加载，全程显示阶段进度提示（"正在下载 xx%""正在解压…""正在加载…"），完成后立即生效
  - 插件随 GitHub Release 自动构建分发，支持随时安装/卸载

---

---

## 8.43.1（2026-08-31）

**修复**
- **动作编辑弹窗保存按钮被软键盘遮挡**：修复真机上"点击按钮"等动作配置弹窗输入时，软键盘（尤其中文键盘）遮挡底部保存按钮导致无法保存的问题。
  - NotixDialog 外层增加 imePadding，软键盘弹出时弹窗自动上移到键盘上方
  - 弹窗内容区增加垂直滚动，高度收缩时仍可滚动到保存/确认按钮
  - 属通用修复，所有带输入框的弹窗（按钮名、TTS 模板、延迟等）一并受益

---

---

## 8.43.0（2026-08-31）

**新增**
- **通知热词词云**：统计页新增通知热词词云卡片，通过阿基米德螺旋布局 + 碰撞检测展示高频词，支持今日/本周/本月/全部四个时间范围切换。
- **词频数据库**：新增 word_frequency 表，新通知到达时增量更新词频（每条通知 <10ms，不影响监听性能）。
- **分词插件架构**：内置简单分词器 + 可选 HanLP 高级分词插件（动态加载，约 5MB），失败自动回退内置分词。

**改进**
- **删除联动**：删除单条通知时词频同步递减，清空全部历史时词频表同步清空。
- **设置项**：新增"每日重建词频"开关（默认关闭，每天凌晨3点全量重建防止增量误差）和"高级分词插件"管理入口。

**技术**
- 数据库升级到 v5，新增 word_frequency 表。

---

---

## 8.42.2（2026-08-31）

**改进**
- **常驻通知内容去重**：常驻通知（音乐播放、导航等）连续更新时，如果标题和内容与上一条完全相同，只刷新时间戳，不增加变更计数、不创建重复记录。下载进度等内容变化的通知不受影响，正常计数。

---

---

## 8.42.1（2026-08-31）

**新增**
- **通知详情弹窗显示渠道和 sbnKey**：通知详情弹窗正文下方新增显示通知渠道 ID（channelId）和通知唯一标识（sbnKey），便于调试和后续按渠道聚合分析。
- **数据库升级到 v4**：notification_change 表新增 channel_id 字段，通过正式迁移（ALTER TABLE ADD COLUMN）保留历史数据。

---

---

## 8.42.0（2026-08-31）

**新增**
- **常驻通知更新限流**：同一个常驻通知（如下载进度、音乐播放）30秒内只刷新一次时间戳，期间的更新只增加计数，不触发UI刷新和列表重排。大幅减少频繁更新导致的数据库写入和耗电（一天10000次更新 → 实际仅约120次UI刷新）。

**修复**
- **常驻通知聚合真正根因修复**：saveOngoingNotification 中创建/更新常驻通知组时从未正确设置 was_ongoing = 1（之前的字符串替换均失败），导致常驻通知组的 was_ongoing 始终为默认值0，getLatestNormal() 把常驻通知误判为普通通知返回，微信等同标题普通通知在常驻通知发送期间无法聚合。现已在新建和更新路径都正确设置 was_ongoing = 1。

---

---

## 8.41.4（2026-08-31）

**修复**
- **常驻通知聚合修复（真正根因修复）**：v8.41.3 的数据修复方法 ixOngoingGroups() 被错误地放在了 if (legacyBlocked.isNotEmpty()) 代码块内，导致大多数用户（没有旧被过滤通知需要迁移）从未执行过该修复，旧常驻通知组的 was_ongoing 字段始终为 0，getLatestNormal() 仍把常驻通知误判为普通通知返回。
  - 将 ixOngoingGroups() 调用移到 if 块外部，确保每次启动都会执行
  - getLatestNormal() SQL 增加双重排除：不仅排除 was_ongoing=1 的组，还通过子查询排除"组内存在 was_ongoing=1 的 change"的组，兼容旧数据未修复的情况

---

---

## 8.41.3（2026-08-31）

**修复**
- **常驻通知聚合修复（根因修复）**：历史迁移（HistoryMigration.kt）时创建的常驻通知组未设置 was_ongoing 字段，默认值为 0，导致 getLatestNormal() 把常驻通知误判为普通通知返回，微信等同标题普通通知在常驻通知发送期间无法聚合。
  - 修复迁移逻辑：迁移时根据 change 的 wasOngoing 字段正确设置组的 was_ongoing
  - 新增数据修复：应用启动时自动调用 ixOngoingGroups()，把"组内有 was_ongoing=1 的 change"的组标记为常驻通知，修复已升级用户的旧数据

---

---

## 8.41.2（2026-08-31）

**改进**
- **常驻通知聚合查询优化**：从"查询前50条再在代码中过滤常驻通知"改为"数据库层直接查询最新的一条普通通知"。新增 getLatestNormal() DAO 方法（WHERE was_ongoing = 0 ORDER BY last_timestamp DESC LIMIT 1），无论有多少常驻通知都只查1条普通通知，性能更好，逻辑更简洁。

---

---

## 8.41.1（2026-08-31）

**修复**
- **常驻通知聚合优化修复**：扩大普通通知聚合查询范围从5条到50条，确保在多个常驻通知（下载、音乐、导航等）频繁更新的场景下，仍能正确跳过所有常驻通知找到第一个普通通知组进行聚合判断。同 app 同标题的普通通知（如微信消息）现在能正常聚合显示变更计数，不被常驻通知打断。

---

---


## 8.41.0（2026-08-31）

**改进**
- **常驻通知聚合优化**：普通通知（如微信）聚合判断时跳过常驻通知聚合组，不再被频繁更新的常驻通知（如下载进度、音乐播放）打断连续性。同一标题的微信消息即使中间隔了常驻通知更新，仍能正常聚合显示变更计数。

**技术**
- NotificationGroupEntity 新增 was_ongoing 字段标记常驻通知聚合组。
- 数据库版本升级到 v3。

---

## 8.40.0（2026-08-31）

**新增**
- **统计页第一波**：通知趋势折线图（近7天，Canvas绘制折线+渐变填充+数据点）、24小时分布热力图（近7天×24小时网格，点击查看详情）。
- **统计页第二波**：App通知占比环形图（甜甜圈图，前5app单独显示，其余归为"其他"）、规则效果排行（按命中次数降序，横向进度条可视化）。
- **统计页第三波**：通知健康度评分（0-100分，环形进度条+等级标签）、评分维度详情（7日平均通知量、来源分散度、规则过滤效率）、个性化改进建议。

---

## 8.39.0（2026-08-30）

**新增**
- **统计页**：底部导航新增第4个tab「统计」，展示通知总量（今日/本周/本月）、已过滤总数、规则命中总次数、App通知排行（前5）等基础统计数据。
- **规则合并提示**：创建规则时，若存在除关键字外其他条件（应用、手机状态、时间、动作流、匹配模式）完全相同的已有规则，弹出合并提示。
- **规则卡片分割线**：规则卡片条件区（关键字/状态/时间）与动作流之间增加分割线。

**改进**
- **回顶部动画**：规则页、设置页单击底部tab回顶部改为平滑动画，与历史页保持一致。

**修复**
- **统计页内容空白**：竖屏分支（else分支）有独立的Box结构且未调用screenContent()，导致统计页缺失。现已在两处都补上。
- **设置页回顶部不生效**：底部导航从3个tab改为4个后，设置页index从2变为3，但回顶部条件判断仍写index == 2。已修正为index == 3。

---

## 8.38.0（2026-08-30）

**新增**
- **自定义紧凑Switch组件**：替换系统默认Switch，关闭态也绘制胶囊外形（不填充），整体更紧凑统一。

**改进**
- **规则卡片布局优化**：规则名称字号放大，标题行与app名称行之间增加分割线，整体布局更紧凑。

---

## 8.37.0（2026-08-30）

**改进**
- **规则卡片布局优化**：进一步调整规则卡片标题行和间距，更紧凑。

---

## 8.36.0（2026-08-30）

**改进**
- **规则条件关键字输入优化**：从带加号按钮的小输入框改为大尺寸可点击卡片，点击弹出对话框输入关键字，标签显示在卡片下方。

---

## 8.35.0（2026-08-30）

**改进**
- **规则卡片布局优化**：规则描述区改为3行（关键字、手机状态、时间），不限则不显示该行。
- **存储占用弹窗修复**：修复文件列表错误（移除过时的"history.json"条目），更新大小计算和文案。

---

## 8.34.0（2026-08-30）

**新增**
- **通知详情弹窗持久性类型显示**：标题区显示"常驻通知"或"普通通知"，主题色+粗体。
- **已过滤tag移到标题行**：通知详情弹窗的"已过滤"标签移到持久性类型右侧，间隔4pt，同一行显示。

---

## 8.33.0（2026-08-30）

**修复**
- **已过滤tab空状态上滑bug**：修复"已过滤"tab在无卡片时仍能过度上滑（柱状图滚出屏幕）的问题。
- **规则/设置页单击底部tab回到顶部**：在规则页或设置页时，再次点击底部tab可滚动回顶部。

---

## 8.32.0（2026-08-30）

**改进**
- **应用信息刷新优化**：设置页"刷新应用信息"改为立即通过PackageManager重新获取，而非清除缓存。
- **未监控应用状态同步**：在应用分组中停止监控某app后，设置页的未监控应用列表立即刷新。

---

## 8.30.0（2026-08-30）

**修复**
- **底部tab切换滚动位置丢失**：切换tab时不再销毁重建History页面，保留滚动位置和折叠/展开状态。

---

## 8.29.0（2026-08-30）

**修复**
- **底部tab切换滚动位置丢失**：初步修复切换tab时滚动位置丢失的问题。

---

## 8.28.0（2026-08-29）

**修复**
- **规则应用到现有通知时正确标记历史为已过滤**：新启用规则移除通知栏现有通知时，全局去重路径跳过了更新locked标志，导致"已过滤"tab为空。现已更新匹配sbnKey+postTime的现有记录的blocked状态为1。

---

## 8.27.0（2026-08-29）

**新增**
- **规则自动应用到通知栏现有通知**：创建、更新或启用规则时，会对当前所有活跃通知执行一次匹配和动作执行（移除等），无需等待新通知到达。此前仅评估新到达的通知。

---

## 8.26.0（2026-08-29）

**修复**
- **全局去重修复BUG-001重复通知组**：按sbnKey+postTime全局去重，修复相同通知不在头部时重复入库的问题。

---

## 8.25.0（2026-08-29）

**新增**
- **重连时同步活跃通知**：通知监听服务重连时同步当前通知栏的活跃通知到历史。
- **移除3秒防抖**：移除服务层3秒防抖，避免漏掉聊天消息。

---

## 8.24.0（2026-08-29）

**技术**
- 版本升级，包含多项稳定性改进。

---

## 8.23.0（2026-08-29）

**新增**
- **搜索上限500条**：搜索结果上限调整为500条。
- **自动清理旧JSON**：自动清理过期的JSON历史数据。
- **存储统计包含Room数据库**：存储占用统计现在包含Room数据库文件。

---

## 8.22.0（2026-08-29）

**新增**
- **通知历史迁移到Room数据库**：从JSON存储迁移到Room数据库，提升查询性能和数据完整性。
- **常驻通知聚合**：常驻通知（ongoing）按sbnKey全局聚合，修复重复卡片bug。

---

## 8.21.0（2026-08-29）

**技术**
- 版本升级，包含多项稳定性改进。

---

## 8.20.0（2026-08-29）

**技术**
- 版本升级，包含多项稳定性改进。

---

## 8.19.0（2026-08-29）

**技术**
- 版本升级，包含多项稳定性改进。

---

## 8.18.0（2026-08-29）

**改进**
- **通知取色引擎优化**：8项优化（高+中优先级），提升动态取色准确性和性能。

---

## 8.17.0（2026-08-29）

**改进**
- **页面标题统一**：通知历史、规则、设置页标题字体样式统一。
- **设置页吸顶标题**：设置页上滑时标题吸顶。
- **规则页布局优化**：规则页标题位置与设置页一致。

---
## 8.16.0（2026-08-29）

**改进**
- **通知卡片更紧凑**：内边距从 16dp 收紧为 12dp，行间距从 4dp 收紧为 2dp。在 420dpi 设备上，典型中文卡片（App 名 + 标题 + 摘要 + 时间）从 319px 降至约 280px（107dp）。字体大小不变，一屏可显示更多卡片。
- **通知卡片间垂直间距统一**：卡片现在使用 2dp 垂直内边距，与应用分组头之间的间距一致，列表节奏在分组与非分组区域统一。
- **折叠展开区卡片现在显示通知标题**：此前折叠组展开后的体卡片使用紧凑模式隐藏了标题；现在显示完整标题，同时保留水平缩进来区分层级。

**修复**
- **移除聚合卡片底部冗余的「其余 N 条」行**：右上角计数徽标已传达总数，底部行为重复信息，已移除。
- **修复空列表或内容较少时历史 tab 出现大量空白可滚动区域的问题**：移除了占满一屏的 `scroll_room` 占位项和额外的 240dp 底部 contentPadding 余量；内容足够时图表仍自然滑出，内容不足时不再过度滚动到空白区。

**说明**
- `STRONG_REMIND` 与 `POSTPONE` 仍为仅 UI 空壳。

---

## 8.15.2（2026-08-25）

**修复**
- **动态强调色不再对「无法解析图标的 App」塌成单一中性灰。** 来自当前设备无法解析图标的应用（已卸载、仅单色/自适应图标、或包名缺失）的通知，原先统一回退到同一种中性灰，导致历史卡片与分组头难以区分。现改为按包名哈希得到的确定性兜底色，每个 App 仍保留可辨识的颜色。

**改进**
- **分组头改用与通知卡片相同的「背景派生」强调色**，统一历史页取色来源（原先分组头与卡片用了引擎不同字段）。
- **`NotificationColorEngine` 新增诊断日志**：每种取色回退（包名为空 / 图标未找到 / 单色图标）现在都会输出带包名的 `Log.w`，便于在真机排查失色问题。

**说明**
- `STRONG_REMIND` 与 `POSTPONE` 仍为仅 UI 空壳。

---

## 8.15.1（2026-08-25）

**修复**
- **历史页：点击搜索按钮不再误切换子 tab。** 搜索与 tab 头部原先嵌套在 `HorizontalPager` 的每一页内，展开搜索会扰动 pager 布局并 settle 到错误页，进而把选中 tab 锁死。现头部统一渲染在 pager 上方，搜索展开/收起不再影响 tab 选择。

**说明**
- `STRONG_REMIND` 与 `POSTPONE` 仍为仅 UI 空壳。

---

## 8.15.0（2026-08-25）

**新增**
- **设计系统基座**：一套统一的 Notix 主题令牌（颜色、间距、字体、圆角、布局）现在驱动所有页面，替代原先零散硬编码的取值。新增内置设计系统预览页，供开发与 QA 使用。
- **可复用组件库**：公共构件（`SectionHeader`、`NotificationCard`、`FilterChip`、各类弹窗组件等）已集中沉淀，并在历史、规则、设置与规则向导中统一复用。

**改进**
- **历史页**完成令牌化，并基于可复用卡片组件重建；间距与深色/浅色主题已实机验证。
- **规则页**迁移到共享组件集。
- **设置页**完成令牌化并切换到共享组件。
- **规则向导**端到端完成令牌化。
- **弹窗系统统一**：全站统一为 `NotixDialog` 风格（条件配置、匹配模式选择、确认、关于等），尺寸、圆角、遮罩与点击外部关闭行为一致。

**变更**
- 浅色与深色主题现已共享同一套令牌驱动的配色；全 App 在真实设备上通过 5 条端到端流程的可视化回归。

**说明**
- `STRONG_REMIND` 与 `POSTPONE` 仍为仅 UI 空壳。

---

## 8.14.1（2026-08-25）

**修复**
- **仅 release 包：「包括常驻通知」冻结时长 chip 无法选中。** 在 release 构建（R8 全量优化）下，用于冻结时长的 `Long` 状态在 `FilterChip` 内部比较异常，导致点击时长 chip 无反应、没有任何 chip 保持选中。UI 层现在将选中时长改为 OPTIONS 列表的索引，绕过 R8 下 `Long` 装箱/比较问题。

**说明**
- `STRONG_REMIND` 与 `POSTPONE` 仍为仅 UI 空壳。

---

## 8.14（2026-08-24）

**新增**
- **常驻通知冻结时长可自定义**：「移除」动作的「包括常驻通知」开关现在提供时长选择（1 小时 / 1 天 / 7 天 / 30 天 / 1 年，默认 7 天）。被冻结的常驻通知在时长到期后自动回到通知栏。
- **设置页「恢复常驻通知」**：在「规则与数据」分区新增入口，一键恢复所有被规则冻结的常驻通知，带确认弹窗。

**修复**
- v8.13 用极大值（约 146 年）冻结常驻通知。由于 Android 11+ 上 snooze 跨重启持久化、且公开 API 无 un-snooze，删除规则后通知几乎永久消失。v8.14 修复：冻结时长改为用户可选（默认 7 天）；删除规则时自动恢复该规则冻结的通知；恢复采用对同一 key 的 100ms 短时长 re-snooze（覆盖原到期时间）。

**改进**
- 「移除」动作摘要现在显示已配置的冻结时长（如「冻结 7 天」）。

**说明**
- 「包括常驻」路径使用 `snoozeNotification`（API 26+）；旧系统降级到 `cancelNotification`。
- Android 11+ 上 snooze 跨重启持久化；恢复方式为时长自然到期、删除规则、或设置页新入口。
- `STRONG_REMIND` 与 `POSTPONE` 仍为仅 UI 空壳。

---

## 8.13（2026-08-24）

**新增**
- **「移除固定/常驻通知」**：在「移除」动作的弹窗中新增「包括常驻通知」开关。开启后，对系统无法用 `cancelNotification` 直接消掉的通知（如音乐播放、前台服务、系统「正在其他应用的上层显示内容」提示），改走 `snoozeNotification(key, 极大值)` 进行冻结，行为与通知滤盒/BuzzKill 一致。**默认关闭**，保持原有行为。

**改进**
- 规则向导 DISMISS 动作卡片在动作流摘要中区分「移除通知」与「移除通知（含常驻）」，规则列表一眼可见是否覆盖常驻通知。

**说明**
- 「包括常驻」路径使用 `snoozeNotification`，仅在 Android 8.0（API 26）及以上系统可用；旧系统静默降级到 `cancelNotification`。
- 冻结的通知在手机重启后会重新出现（系统限制）；通知滤盒里「把手机时间往回调 1 年」的恢复技巧对 Notix 同样有效。
- `STRONG_REMIND`（强提醒）与 `POSTPONE`（延迟）动作在 v8.13 中仍为仅 UI 的空壳；其真实执行将在未来版本落地。

---

## 8.12（2026-08-24）

**改进**
- 规则「配置条件」弹窗升级为全站统一的 `NotixDialog` 风格（原为 `AlertDialog`）：固定 520dp 高，内部三标签页（关键字 / 手机状态 / 时间）内容区可滚动。
- 匹配模式选择从内嵌 `DropdownMenu` 改为独立的 `NotixDialog`：滚动列表、选中项显示对勾圆圈、行间分隔线、`ADVANCED` 显示为禁用。
- 关键字输入移入点击触发按钮弹出的 `NotixDialog`；MIXED「包含A且不包含B」模式展示两个输入区（包含 A / 且不包含 B），其他模式只展示一个「包含关键字」输入区。

**变更**
- 关键字以 chip 形式直接显示在条件界面；点击 chip 打开弹窗并回填编辑，尾部 × 直接删除。
- 关键字输入弹窗：去掉内嵌「+」按钮；底部「确定」按钮改为提交当前文本并关闭；输入框支持长文本换行（最少 2 行 / 最多 5 行）。

**说明**
- `STRONG_REMIND`（强提醒）与 `POSTPONE`（延迟）动作在 v8.12 中仍为仅 UI 的空壳；其真实执行（高优先级 heads-up + 响铃/震动、延迟重投）将在未来版本落地。

---

## 8.11（2026-08-24）

**改进**
- 规则动作流支持拖拽排序的端到端动画：被拖动的卡片抬起并轻微倾斜，其余卡片让出空间，松手时以弹簧动画归位。基于 `sh.calvin.reorderable` 实现生产级拖拽手势。

**修复**
- 移除对已删除 `SILENT` 动作的死代码路径：`SyncActionRunner.silent`、`ActionFlowHost.repostSilent`、`NotificationBlockerService.repostSilent`；测试桩与断言同步更新。

**说明**
- 强提醒（`STRONG_REMIND`）与延迟（`POSTPONE`）动作在 v8.11 中仍为仅 UI 的空壳；其真实执行将在未来版本落地。

---

## 8.10（2026-08-24）

相对上一发布版本 8.9 的变更。

### 改进
- 规则向导：点击动作卡片现在打开与全站 `NotixDialog` 风格一致的设置弹窗（带 X 关闭的标题栏、92% 宽、点遮罩关闭），取代原先卡片下方的内联展开。
- 「添加动作」选择器改为与其他应用内弹窗一致的 `NotixDialog`。底部「取消」按钮移除；点 X、点遮罩或使用系统返回手势关闭。
- 每个动作卡片的拖动手柄改为独立的 48 × 64 dp 触控列，内嵌 28 dp 图标，与点击打开区域视觉分离，避免点按/长按手柄误触打开设置弹窗。
- 规则向导的来源 App 选择器：搜索框右侧新增上下箭头用于折叠/展开 App 列表，展开/折叠状态在配置变更后保留。

### 变更
- 动作目录按 Notix v1 规划重新对齐：原有 7 个动作重塑为精选的 8 个——移除、点击按钮、打开、复制内容、TTS 播报、强提醒、等待、延迟。标签改名为约定好的用户面向名称（如「消除通知」→「移除」、「打开通知」→「打开」、「播报」→「TTS 播报」）。「静默重显」动作被移除。
- 本版本新增动作在 UI 层已完整且可安全保存：其参数（强提醒响铃/震动开关、延迟毫秒）可设置并持久化。强提醒（heads-up + 响铃 + 震动）与延迟（延迟重投）的真实运行时执行计划于 v8.11 落地；当前仅记录为 skipped，动作流继续而不崩溃。

### 修复
- 动作卡片点按拖动手柄不再触发设置弹窗，因为打开弹窗的点击目标与拖拽手势目标现已互斥。
- 规则向导中弹窗样式不一致：「添加动作」选择器与各动作设置弹窗现已与设置页、权限管理、存储占用弹窗同属 `NotixDialog` 家族。

---

## 8.9（2026-08-23）

相对上一发布版本 8.8 的变更。

### 改进
- 权限管理：每张权限卡片现可点击并直接跳转对应系统设置页（通知使用权 → 通知监听设置、发送通知 → 应用通知设置、电池优化 → 电池优化豁免、前台服务 → 应用详情）。卡片上的箭头表示入口。
- 权限管理页现列出所有应用权限，并附其 Android 常量名及使用的确切组件，使每条已授权/所需权限在一处完整说明。

### 变更
- 移除受限的 `QUERY_ALL_PACKAGES` 权限及清单中 32 条未使用的包可见性条目。应用图标与应用名来自捕获的通知，而非全量包扫描，行为不变的同时消除了 Play 商店受限权限审核风险。
- 统一弹窗尺寸：所有基于 `NotixDialog` 的弹窗现采用宽而紧凑的布局（92% 宽、85% 高上限），与通知详情弹窗一致，支持点遮罩关闭。权限弹窗一屏容纳全部四张卡片。

---

## 8.8（2026-08-23）

相对上一发布版本 8.7 的变更。

### 改进
- 统一设置页所有弹窗为与崩溃日志弹窗相同的视觉风格：全应用一致的背景、按钮布局、间距、标题与正文排版。
- 为每个删除/清除操作（通知、规则、规则动作、按全部/日期范围/应用清除历史、停止监控、暂停监听、清除崩溃日志）新增二次确认弹窗。确认采用统一风格，含明确的确认 / 取消按钮；仅确认后执行。

### 修复
- 弹窗按钮现始终单行（行过宽时每行一个）而非换行。
- 长弹窗标题不再截断，安全换行到第二行。
- 按日期范围清除与按应用清除的确认改用专用字符串，而非复用「清除全部历史」措辞（已加入所有支持语言）。

---

## 8.7（2026-08-23）

相对上一发布版本 8.6 的变更。

### 改进
- 设置：权限管理与存储占用现以居中弹窗展示，而非全屏子页，主设置页保留在弹窗后方可见。权限管理入口显示实时状态标签（绿色「正常」/ 红色「N 个问题」）。
- 存储占用弹窗现以总用量汇总头部打开。

### 变更
- 统一弹窗关闭方式：点遮罩或关闭（X）按钮关闭；弹窗内点击不再关闭。

---

## 8.6（2026-08-23）

相对上一发布版本 8.5 的变更。

### 改进
- 设置权限整合重构：原散落在主设置页的「通知监听 / 电池优化 / 后台保活」指南合并为单一「权限管理」子页。主设置页对应区域变为单行可点击入口行，带盾牌图标、状态徽标（绿色「正常」/ 红色「N 个问题」）及右箭头；点击打开子页，展示 4 个系统权限的实时状态、说明与修复入口。
- 新增「发送通知（POST_NOTIFICATIONS）」与「前台服务保活」的实时监控，连同已有的「通知监听」与「电池优化」构成 4 个监控项。

### 修复
- 权限状态卡片卡在初始值：原实现用一次性 `remember {}` 求值，从系统设置返回后状态不再刷新。改为 `refreshTick` + `DisposableEffect(Lifecycle.ON_RESUME)`，返回前台/恢复时自动重新检查，在主入口与子页间同步。

### 清理
- 移除 6 个未使用的 `settings_keepalive_*` 字符串（此前随旧内联权限指南一同废弃）。

---

## 8.5（2026-08-23）

相对上一发布版本 8.4 的变更。

### 修复
- 通知未实时进入历史：H3 引入的内存缓存是实例级字段，监听服务写入后仅自身缓存刷新，UI 实例缓存停留在首次读取的陈旧值；广播刷新命中陈旧缓存，历史停止更新。将缓存提升为进程级（companion object）共享，任意一次磁盘写入立即可见（重大回归修复）。
- 崩溃日志弹窗「打开日志位置」按钮文字换行：Material3 按钮默认内边距过大，半宽按钮放不下 6 字标题。收紧内边距并强制单行。
- 崩溃日志弹窗「打开日志位置」点击无反应：原逻辑用 FileProvider 以 `text/plain` 打开日志文件（非目录），无可用查看器时报错。改为优先用系统文件管理器打开日志目录，回退打开文件，最后失败时显示路径。

---

## 8.4（2026-08-23）

相对上一发布版本 8.3 的变更。

### 改进
- 历史存储切换为原子写入（AtomicFile）+ 后台线程即时增量刷新 + 节流的历史更新广播，消除大历史文件下的主线程解析卡顿 / ANR 风险。
- 监听服务环境快照缓存 10s，避免高频通知到达时重复注册电池广播与枚举蓝牙音频设备，降低监听线程负载。

### 修复
- 历史折叠段展开状态按段级 id 隔离，修复同包多个折叠段被一起展开/折叠的问题。
- 折叠提示计数错误：隐藏计数现正确减去始终显示的首项，例如 4 个折叠项显示「折叠 3」。
- 设置向导因旋转 / 配置变更被迫中途退出：重建时优先尊重已保存的向导可见状态（M1）。
- 动作流宿主（监听服务）销毁后挂起回调仍推送副作用：注入宿主存活检查，销毁后静默终止（M2）。
- 规则导入提示不准确：新增明确的「无新规则（已存在全部）」提示，避免误导成「已导入 0」（L1）。
- 并发读写数据丢失：StatsStorage 计数自增与 UnmonitoredAppsStorage 增/删改为加锁读改写，修复高频命中 / 并发增删互相覆盖导致的计数丢失（L2）。

---

## 8.3（2026-08-23）

相对上一发布版本 8.2 的变更。

### 改进
- 通知详情弹窗加宽至真实屏幕宽的 90%（关闭平台默认窄窗口，原被压缩到 80% 以下）。
- 弹窗交互修复：点遮罩关闭、恢复半透明遮罩、弹窗内点击不再误关。
- 聚合窗口（变更详情）同步为 90% 宽、点遮罩关闭，与详情弹窗视觉一致。
- 全应用深色模式颜色标准化为 Material Design 3 标准深色：基面由纯黑改为 `#1B1B1F`、主色 `#92CCFF`、文字/分隔线遵循 MD3 中性色板，对比度达 WCAG AAA。
- 严格保留应用分组卡片、规则分组卡片、规则卡片既有的应用派生颜色——未做颜色改动。

### 修复
- 通知详情弹窗无法点外部关闭（关闭平台默认窗口后原生遮罩失效；新增手动遮罩）。
- 仓库中缺失 `Shape.kt`（`NotixCorner` 圆角令牌）导致引用错误、发布构建失败。
- Windows 上 Gradle daemon JVM CompressedOops 地址冲突（抬高堆上限并将基址迁移到 4GB 以上）。

---

## 8.2（2026-08-23）

相对上一发布版本 8.1 的变更。

### 改进
- 统一按钮系统：通知详情弹窗的四个按钮改为 Material3 Button 系统（删除红色填充 / 打开主题色 / 恢复灰色填充 / 创建规则描边）；规则页、向导页、编辑页按钮统一为 14dp 圆角。
- 统一危险操作与圆角层级：两种危险变体（红色填充按钮 / 红色文字 TextButton）全项目一致；圆角统一为大容器 16dp、卡片 12dp、小控件 8dp。
- 统一标题层级与弹窗：主页 headlineMedium+Bold（设置页加主标题），子页 titleLarge+返回；合并冗余弹窗组件。
- 图标规范化：周视图 ◀/▶ 与遗留 Unicode 装饰字符替换为矢量图标。

### 修复
- 移除设置页日期快捷入口与列表项之间不一致的描边按钮变体。
- 合并通知详情弹窗中的冗余组件，删除未使用的 NotificationDetailsDialog。

---

## 8.1（2026-08-22）

相对上一发布版本 8.0 的变更。

### 改进
- 历史页多级吸顶：底部 tab 固定吸顶，统计区与折叠分组头吸顶在 tab 下方，滚动时不再把标签栏推出。
- 规则向导简化：移除底部删除键、动作箭头、添加条件按钮；条件区固定三行，新建规则更聚焦。
- 通知详情弹窗加宽（侧边距缩至 12dp），固定显示删除 / 打开 / 创建规则 / 恢复四个按钮。
- 启动图标重做：替换为自适应矢量图标（三横灰）并微调缩放。

### 修复
- 通知历史列表布局顺序错误。
- 折叠段展开/折叠滚动位置错误：折叠后自动回到段头。
- 同应用多个折叠段折叠计数错误。

---

## 8.0（2026-08-22）

相对上一发布版本 7.47 的变更。

### 新功能
- 存储占用：设置 > 通用新增「存储占用」入口与二级详情页，按类别展示通知历史 / 规则 / 其他文件大小，支持逐项清除与一键清除全部（均带确认）。
- 按应用清除历史：弹窗改为深色卡片风格；列表项现显示应用图标，支持行点击多选，搜索过滤后一键全选 / 清除选择。
- 按时间范围清除历史：弹窗改为深色卡片风格；支持起止日期选择加快捷预设（7 / 30 / 90 天 / 全部）。
- 规则创建拆分为三栏：规则页分为「来源 / 条件 / 工作流」；规则名可填于顶部（为空时自动命名为「未命名规则 N」）；条件通过弹窗配置（关键字 / 手机状态 / 时间标签页）；动作卡片支持拖拽排序、点击配置、长按删除。
- 底部三 tab（历史 / 规则 / 设置）过渡动画：淡入 + 轻微水平位移（约 250ms），各页状态保留。

### 改进
- 规则页：移除浮动新建按钮，改为「规则」主标题 + 「共 N 条规则」副标题 + 顶部横幅新建按钮。规则卡片支持长按删除确认；卡片顶部删除按钮移除，保留重新扫描与开关；空状态保留标题与横幅按钮。
- 通知详情弹窗加宽（侧边距缩至 12dp），固定显示删除 / 打开 / 创建规则 / 恢复四个按钮。
- 聚合变更窗口：右上标签由「变更 n」改为「n 处变更」。
- 通知历史柱状图提取为独立圆角深灰卡片，新增 ◀/▶ 滑动提示箭头（横屏左栏同步）。
- 通知历史搜索框展开时自动弹出键盘，关闭时收起。
- 底部 tab 选中态改为主题色圆角药丸（约 52×32dp，16dp 圆角），图标切换为对比色。

### 修复
- 展开/折叠后通知折叠卡滚动位置错误：折叠后自动回到段头。
- 同应用多折叠段键冲突导致列表 IllegalArgumentException 崩溃。
- 按应用折叠段现按通知发送时间排序，避免 tab 切换卡顿。

---

## 7.47（2026-08-21）

### 修复
- 修复同应用出现在多个非连续折叠段时，因重复折叠切换键导致的 LazyColumn 崩溃（IllegalArgumentException）；折叠段键现包含段内最新条目 id。
- 修复历史 tab 切换卡顿：从过滤 tab 切到按时间 / 按应用时，不再冻结约 1 秒才显示目标列表。数据准备与所选 tab 解耦，三个 tab 共享一份缓存数据集，切换不触发重算。

### 改进
- 按应用折叠现按发送时间分段：同应用组内，仅当在全局时间线中相邻时才折叠在一起。若同应用两条之间插入了其他应用的通知，则构成独立折叠段而非一个大折叠组。

---

## 7.46（2026-08-21）

### 新功能
- 历史页新增通知卡片折叠：某应用有 4+ 条连续聚合条目时，折叠为单卡片并带「显示 N 条更多」开关；适用于全部三个 tab（按时间 / 按应用 / 已过滤）。展开/折叠开关吸顶在最新条目下方，一键折叠；展开卡片轻微缩进以区分普通卡片。

### 改进
- 为新增折叠字符串在西班牙语、法语、日语、韩语、波兰语、俄语中加入翻译。

---

## 7.45（2026-08-20）

### 新功能
- 新增可选「提取 remote views 文本」开关（默认关）：开启后，无可见文本的通知将提取其动作按钮标签与内容描述，用于规则匹配与历史记录。

### 修复
- 修复历史页「已过滤」与「按应用」tab 间滑动卡顿：分组/排序计算现缓存，不再每次重组全量重算。
- 修复 tab 切换或列表回收后应用图标延迟出现：新增应用图标进程内内存缓存，避免重复命中 PackageManager。

---

## 7.43（2026-08-19）

### 移除
- 移除不完整的社区分享功能：删除 CommunityShare.kt 及其在全部 8 个语言区的 80 条相关字符串。

---

## 7.42（2026-08-18）

### 新功能
- 遵循 Material Design 3 深色色板的纯黑（OLED 友好）深色主题。
- 底部导航栏压缩至 56dp，仅图标项；长按图标显示其标签。
- 全部三个 tab 统一横屏布局：左侧固定图表面板，右侧页面内容，由垂直分隔线分隔。

### 改进
- 横屏模式下选中日期状态跨旋转保留（共享图表面板）。
- 相邻历史子 tab 预组合，避免滑动时空白闪烁。
- 底部 tab 现仅点击切换，顶部子 tab 仍支持滑动。

### 修复
- 修复横屏屏显示旧灰窗口背景而非纯黑。

---

## 7.39（2026-08-18）

### 新功能
- 历史 tab 现支持三个子 tab 间滑动导航（HorizontalPager）。
- 「已过滤」tab 按命中的规则分组通知记录。

### 改进
- 即便通知很少，历史列表仍可滚动（修复内容短时无法滚动的问题）。
- 双击底部导航的「历史」tab 快速返回本周（清除日期过滤）。

### 修复
- 修复屏幕旋转时规则向导状态丢失。

---

## 7.38（2026-08-18）

- 初始版本说明条目。完整变更日志：https://github.com/Enlpot/Notix/commits/v7.38
- 通知详情弹窗按钮显示异常。

